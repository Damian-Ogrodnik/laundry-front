import React from "react";

export const UserBoard = () => {
  const logOut = async () => {};

  return (
    <div>
      User Board <button onClick={() => logOut()}>Log out</button>
    </div>
  );
};
